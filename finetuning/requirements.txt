# Finetuning Requirements
# Choose ONE method based on your hardware

# ============================================================================
# Method 1: Unsloth (Recommended - Fastest)
# ============================================================================
# Requires: CUDA GPU (8GB+ VRAM)
# Install with: pip install -r requirements.txt

# Unsloth (install from git)
# Run: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Core dependencies for Unsloth
torch>=2.1.0
transformers>=4.36.0
datasets>=2.16.0
trl>=0.7.4
peft>=0.7.0
accelerate>=0.25.0
bitsandbytes>=0.41.3

# ============================================================================
# Method 2: Hugging Face Transformers + PEFT
# ============================================================================
# Requires: CUDA GPU (8GB+ VRAM) or CPU (slow)
# Install with: pip install transformers datasets peft trl bitsandbytes accelerate

transformers>=4.36.0
datasets>=2.16.0
peft>=0.7.0
trl>=0.7.4
bitsandbytes>=0.41.3
accelerate>=0.25.0
torch>=2.1.0

# ============================================================================
# Method 3: MLX (Apple Silicon only)
# ============================================================================
# Requires: Mac with M1/M2/M3 chip
# Install with: pip install mlx-lm

mlx>=0.0.9
mlx-lm>=0.0.13

# ============================================================================
# Optional: WebLLM Conversion
# ============================================================================
# For browser deployment
# Install with: pip install mlc-ai-nightly

mlc-ai-nightly>=0.1.0

# ============================================================================
# Common utilities
# ============================================================================
numpy>=1.24.0
tqdm>=4.65.0

# ============================================================================
# Development (optional)
# ============================================================================
jupyter>=1.0.0
ipywidgets>=8.0.0
matplotlib>=3.7.0
